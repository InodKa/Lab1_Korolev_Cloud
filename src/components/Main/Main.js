export default function Main({children}) {
    return (
      <div style={{
        backgroundColor: '#ffe4e1',
        height: 1000,
        textAling: "center",
        fontSize: 88,
      }}>{children}</div>
    );}